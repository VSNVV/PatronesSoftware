package Strategy;

public class EstrategiaReducida implements Estrategia {
    // Atributos de la clase EstrategiaReducida

    // Métodos de la clase EstrategiaReducida

    @Override
    public double calcular(double precio) {
        // Implementar el algoritmo para calcular la tarifa reducida
        return (precio * (1.10));
    }
}
